apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: parser-deployment
spec:
  replicas: 50
  template:
    metadata:
      labels:
        app: parser
    spec:
      containers:
      - name: parser
        image: TODO
        ports: